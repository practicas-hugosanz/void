RewriteEngine On

# Si el archivo existe (como api/auth.php), cárgalo normalmente
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Si el usuario escribe /admin, carga /admin.php internamente
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^([^\.]+)$ $1.php [NC,L]
