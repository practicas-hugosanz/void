# VOID API — Apache config
# Allow PHP files, block direct access to /includes and /data

Options -Indexes

# Block access to sensitive dirs
<FilesMatch "\.(sqlite|db|log)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
